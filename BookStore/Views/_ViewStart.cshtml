@{
    ViewData["Title"] = "_ViewStart";
}


@{
    var routeUrl = ViewContext.RouteData.Values["Controller"].ToString();
    //you can put pages' name into pages
    var shop = new List<string> { "Shop" };
    var withoutLayout = new List<string> { "Login" };
    //if routeUrl contains any of pages,the page will use _Layout as Layout page,the other pages will use _Layout1 ad Layout page
    if (shop.Any(routeUrl.Contains))
    {
        Layout = "_Layout";
    }
    else if (withoutLayout.Any(routeUrl.Contains))
    {

    }
    else
    {
        Layout = "_AdminLayout";
    }

}
